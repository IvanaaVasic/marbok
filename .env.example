# Configure as encrypted server environment variables in both Vercel projects.
# Rotate the Sanity token that was committed to GitHub before deploying.
SANITY_API_TOKEN=
RESEND_API_KEY=
ORDER_FROM_EMAIL=porudzbine@your-verified-domain.example
ORDER_NOTIFICATION_EMAIL=
NEXT_PUBLIC_SITE_URL=https://marbok.vercel.app
