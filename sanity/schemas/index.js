import page from "./page-schema";
import productBlock from "./product-block-schema";
import productInfo from "./product-info-schema";
import productImages from "./product-images-schema";
import heroImages from "./hero-images-schema";
import mainHeading from "./main-heading-schema";
import brandImages from "./brand-images-schema";

const shcemas = [
  page,
  productBlock,
  productInfo,
  productImages,
  heroImages,
  mainHeading,
  brandImages,
];

export default shcemas;
